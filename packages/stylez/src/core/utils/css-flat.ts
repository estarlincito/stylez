// import { toHash } from '@/core/lib/hash.js';

// import type { Prop, Vars } from '../../types/styles.js';

// const toKey = (value: Prop) => toHash(value, '--z');

// export const toCssFlat = (key: string, value: Prop, vars: Vars) => {
//   const varKey = toKey(value);
//   if (!vars.get(varKey)) vars.set(varKey, value);
//   return [key, `var(${toKey(value)})`];
// };
