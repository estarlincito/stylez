// import type { Vars } from '../../types/styles.js';

// type Prop = string | number;
// export type Flat = Record<string, Prop>;
// export type Nested = Record<string, Flat>;
// export type Props = Record<string, Prop | Flat>;

// // type PropsData = [string[][], (string | Flat)[][]];

// export const toCssProps = (props: Props, className: string, vars: Vars) =>
//   props;
// // const propsData = Object.entries(props);

// // propsData.map(([key, obj]) => {
// //   console.log(obj);
// // });
